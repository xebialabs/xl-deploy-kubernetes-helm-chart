
  overrideValues:
    hooks.getLicense.image.image: $RELATED_IMAGE_GET_LICENSE
    hooks.genSelfSigned.image.image: $RELATED_IMAGE_GEN_SELF_SIGNED
    centralConfiguration.image.image: $RELATED_IMAGE_DEPLOY_CENTRAL_CONFIGURATION
    master.image.image: $RELATED_IMAGE_DEPLOY_MASTER
    master.volumePermissions.image.image: $RELATED_IMAGE_DEPLOY_MASTER_VOLUME_PERMISSIONS
    worker.image.image: $RELATED_IMAGE_DEPLOY_WORKER
    worker.volumePermissions.image.image: $RELATED_IMAGE_DEPLOY_WORKER_VOLUME_PERMISSIONS
    busyBox.image.image: $RELATED_IMAGE_DEPLOY_BUSY_BOX
    postgresql.image.image: $RELATED_IMAGE_POSTGRESQL
    postgresql.volumePermissions.image.image: $RELATED_IMAGE_POSTGRESQL_VOLUME_PERMISSIONS
    rabbitmq.image.image: $RELATED_IMAGE_RABBITMQ
    rabbitmq.volumePermissions.image.image: $RELATED_IMAGE_RABBITMQ_VOLUME_PERMISSIONS
